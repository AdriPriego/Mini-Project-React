

function Favoritos(props) {
  
    /*console.log(props.currentRental.name)
    const favoritos = props.currentRental.filter(eachRental => )
    return (
    <div>
        <h1 id="favoritos">Favoritos</h1>
        <ul>
            {props.currentRental.map((eachRental) => (
                <li key={eachRental.id}>{eachRental.name}</li>
            ))}
        </ul>
    </div>
  )*/


}

export default Favoritos